 $(document).on("click", ".login", function() {
	 // When HTML DOM "click" event is invoked on element with ID "somebutton", execute the following function...
	 console.log("insid func")
               /* $.get("login", function(responseText) { 
                	// Execute Ajax GET request on URL of "someservlet" and execute the following function with Ajax response text...
                	console.log("success");
                	//console.log(responseText)
                    $(".login-error-alert p#p1").text("Incorrect username or password! -----------");           // Locate HTML DOM element with ID "somediv" and set its text content with the response text.
                });*/
            });